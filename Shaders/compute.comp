#version 450
layout(local_size_x = 64) in;
layout(binding = 0) buffer Input { int data[]; } inputBuffer;
layout(binding = 1) buffer Output { float data[]; } outputBuffer;
void main() {
    uint idx = gl_GlobalInvocationID.x;
    outputBuffer.data[idx] = float(inputBuffer.data[idx]) * 4.2;
}